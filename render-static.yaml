services:
  - type: web
    name: doctormation-static
    env: static
    buildCommand: npm ci && npm run build:static
    staticPublishPath: ./static-dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
