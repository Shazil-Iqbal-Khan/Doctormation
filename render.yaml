services:
  - type: web
    name: doctormation
    env: node
    region: oregon
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: node server-start.mjs
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: NODE_ENV
        value: production
