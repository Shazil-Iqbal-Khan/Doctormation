services:
  - type: web
    name: doctormation
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node .output/server/index.mjs
    envVars:
      - key: NODE_VERSION
        value: 18.20.0
